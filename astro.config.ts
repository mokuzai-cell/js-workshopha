import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://s-union.github.io', // https://mokuzai-cell.github.io
    base: 'astro-hands-on', //js-workshopha
});

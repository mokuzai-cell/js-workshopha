import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://mokuzai-cell.github.io', // https://mokuzai-cell.github.io
    base: 'js-workshopha', //js-workshopha
});
